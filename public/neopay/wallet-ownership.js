(()=>{const KEY='neo_wallet_ownership_v1';const enc=new TextEncoder();const hex=b=>[...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('');async function digest(s){return hex(await crypto.subtle.digest('SHA-256',enc.encode(s)))}function challenge(address,fingerprint){const nonce=crypto.getRandomValues(new Uint32Array(4));return `NEO Wallet Ownership\nAddress: ${address}\nSession: ${fingerprint}\nNonce: ${[...nonce].map(n=>n.toString(16)).join('')}\nPurpose: authorize this browser session only` }async function begin(address,fingerprint){if(!address||!fingerprint)throw new Error('Wallet address and unlocked wallet session are required.');const message=challenge(address,fingerprint),id=(await digest(message)).slice(0,24);const state={address,fingerprint,message,challengeId:id,status:'AWAITING_SIGNATURE',createdAt:Date.now()};sessionStorage.setItem(KEY,JSON.stringify(state));return state}function current(){try{return JSON.parse(sessionStorage.getItem(KEY)||'null')}catch{return null}}function clear(){sessionStorage.removeItem(KEY)}window.NEOWalletOwnership={begin,current,clear}})();