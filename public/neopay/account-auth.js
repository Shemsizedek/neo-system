(()=>{const KEY='neo_native_account_session_v1',PROFILE_KEY='neopay-ces-profile';const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function waitDerived(){for(let i=0;i<40;i++){if(window.NEODerivedWallet)return window.NEODerivedWallet;await sleep(100)}throw new Error('Bitcoin derivation module is unavailable.')}function savedWallet(){try{return JSON.parse(localStorage.getItem(PROFILE_KEY)||'{}').walletAddress||''}catch{return''}}function saveDerivedWallet(address){let p={};try{p=JSON.parse(localStorage.getItem(PROFILE_KEY)||'{}')}catch{};if(!p.walletAddress){p.walletAddress=address;localStorage.setItem(PROFILE_KEY,JSON.stringify(p))}const fields=['myWallet','receiveWallet'];fields.forEach(id=>{const el=document.getElementById(id);if(el&&!el.value)el.value=address})}async function unlock(){const input=document.getElementById('neoPassphrase'),status=document.getElementById('authStatus'),phrase=input.value;if(!window.NEOWalletCrypto){status.textContent='Wallet cryptography is still loading. Try again.';return}try{status.textContent='Validating recovery phrase and deriving Bitcoin account locally…';const result=await window.NEOWalletCrypto.unlock(phrase),derived=await waitDerived(),identity=derived.attachSeed(result.seed),expected=savedWallet();if(expected&&expected!==identity.address){derived.clear();window.NEOWalletCrypto.lock();throw new Error(`Derived wallet ${identity.address} does not match the saved NEO account wallet.`)}saveDerivedWallet(identity.address);sessionStorage.setItem(KEY,JSON.stringify({unlocked:true,fingerprint:result.fingerprint,address:identity.address,path:identity.path,at:Date.now(),security:'BIP39 + BIP32 address verified'}));input.value='';document.body.classList.add('neo-unlocked');status.textContent=`Native Bitcoin / Counterparty wallet verified · ${identity.address} · ${identity.path}. Recovery phrase and private key were not saved.`;window.dispatchEvent(new CustomEvent('neo-account-unlocked',{detail:{fingerprint:result.fingerprint,address:identity.address,path:identity.path}}))}catch(e){status.textContent=e.message||'Wallet unlock failed.';input.select()}}function lock(){sessionStorage.removeItem(KEY);window.NEOWalletCrypto?.lock();window.NEODerivedWallet?.clear();document.body.classList.remove('neo-unlocked');const s=document.getElementById('authStatus');if(s)s.textContent='Wallet locked. Signing key cleared from memory.'}document.addEventListener('DOMContentLoaded',()=>{document.getElementById('unlockNeo')?.addEventListener('click',unlock);document.getElementById('lockNeo')?.addEventListener('click',lock)})})();