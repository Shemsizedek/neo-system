export const OLD_COUNTERWALLET_RULES=Object.freeze({hmacKey:'Viacoin seed',basePath:"m/0'/0/",scanCount:10,seedInput:'mnemonic-entropy-hex-string-with-historical-js-bitwise-coercion'});
export function assertOldCounterwalletResult(result){if(!result||result.mode!=='counterwallet-old')throw new Error('Historical Counterwallet mode was not selected.');if(!Array.isArray(result.addresses)||result.addresses.length!==OLD_COUNTERWALLET_RULES.scanCount)throw new Error('Historical Counterwallet recovery must derive exactly the first 10 addresses.');if(result.addresses.some((x,i)=>x.index!==i||x.path!==`${OLD_COUNTERWALLET_RULES.basePath}${i}`||!/^1[1-9A-HJ-NP-Za-km-z]{25,34}$/.test(x.address)))throw new Error('Historical Counterwallet address derivation failed its structural self-test.');return true;}