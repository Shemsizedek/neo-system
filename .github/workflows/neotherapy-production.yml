name: Neotherapy Production Probe

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/neotherapy-production.yml'

permissions:
  contents: read

jobs:
  registration-probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm workflow registration
        run: echo "Neotherapy workflow registration OK"
