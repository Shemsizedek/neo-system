# TEMPLATE ONLY — DO NOT PUT REAL PRIVATE KEYS IN GIT.
# Replace placeholders on the VPN host at deployment time.

[Interface]
Address = 10.144.1.1/16
ListenPort = 51820
PrivateKey = <SERVER_PRIVATE_KEY_FROM_SECRET_STORE>

# Example authorized admin peer.
[Peer]
PublicKey = <ADMIN_DEVICE_PUBLIC_KEY>
AllowedIPs = 10.144.10.10/32

# Add one [Peer] block per authorized device/service.
# Keep AllowedIPs narrow and revoke peers that are lost or no longer authorized.
