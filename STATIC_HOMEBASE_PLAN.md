# NEO Home Base static-first deployment

The public NEO Ecosystem home base is intentionally rendered in plain HTML/CSS first so it remains operational when GitHub Pages serves the repository branch directly or when the application runtime is unavailable. React/Vite remains available for deeper controlled modules and progressive enhancement.
