const transports=['INTERNET','MESH','SMS','RADIO'];const ledger=[];const health=document.querySelector('#health');health.innerHTML=transports.map(t=>`<div class="health"><span>${t}</span><b class="ok">READY</b></div>`).join('');function render(){const el=document.querySelector('#ledger');el.innerHTML=ledger.length?ledger.map(x=>`<div class="entry"><div><b>${x.message_id}</b><br><small>${x.created_at}</small></div><div><b>${x.message.type}</b><br><small>${x.source.neo_id} → ${x.destination.value}</small></div><div>${x.routing.transport_preference[0]}</div><div class="ok">CREATED</div></div>`).join(''):'<p>No demo telegrams yet.</p>'}function envelope(){const now=new Date();return{protocol:'NTP',version:'1.0',message_id:'ntp:'+crypto.randomUUID(),created_at:now.toISOString(),expires_at:new Date(now.getTime()+86400000).toISOString(),source:{neo_id:'NEO-00000144',device_id:'chatgpt-prototype'},destination:{type:'neo_id',value:document.querySelector('#dest').value.trim()},message:{type:document.querySelector('#type').value,priority:'NORMAL',content_type:'text/plain',encoding:'utf-8',payload:document.querySelector('#payload').value},routing:{mode:'AUTO',hop_limit:12,store_forward:true,transport_preference:[document.querySelector('#transport').value]},security:{encryption:'REQUIRED',signature_algorithm:'Ed25519',signature:'DEMO-NOT-CRYPTOGRAPHIC'},ack:{requested:true}}}document.querySelector('#send').onclick=()=>{const x=envelope();ledger.unshift(x);document.querySelector('#inspector').textContent=JSON.stringify(x,null,2);render()};document.querySelector('#clear').onclick=()=>{ledger.length=0;document.querySelector('#inspector').textContent='{}';render()};render();