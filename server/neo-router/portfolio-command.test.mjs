import test from 'node:test';import assert from 'node:assert/strict';import {createPortfolio,prioritizePortfolio,detectResourceConflicts,buildPortfolioCommandView} from './portfolio-command.mjs';
test('ranks objectives and detects cross-campaign capacity conflicts',()=>{const portfolio=createPortfolio({id:'NEO',title:'NEO System',capacity:{software:1,operations:2}});const objectives=[{id:'pay',priority:'critical',strategicValue:80},{id:'router',priority:'high',strategicValue:70}];const campaigns=[{campaignId:'c1',objectiveId:'pay',status:'active',missions:[{workerRole:'software',capacityRequested:1}]},{campaignId:'c2',objectiveId:'router',status:'active',missions:[{workerRole:'software',capacityRequested:1}]}];const priorities=prioritizePortfolio({objectives,campaigns});assert.equal(priorities[0].objectiveId,'pay');const conflicts=detectResourceConflicts({campaigns,capacity:portfolio.capacity});assert.equal(conflicts[0].role,'software');const view=buildPortfolioCommandView({portfolio,objectives,campaigns});assert.equal(view.summary.resourceConflicts,1);assert.equal(view.allocation.allocations.length,2)})
