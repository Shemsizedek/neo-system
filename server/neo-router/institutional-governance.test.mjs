import test from 'node:test';import assert from 'node:assert/strict';import {evaluateAuthority,evaluateQuorum,detectConflicts,createDecisionRecord,appendGovernanceLedger,verifyGovernanceLedger,governanceDecisionView} from './institutional-governance.mjs';
test('enforces authority quorum conflicts and tamper-evident ledger',()=>{const authority=evaluateAuthority({actor:'chair',action:'approve_campaign',delegations:[{id:'d1',actor:'chair',actions:['approve_campaign'],scope:'portfolio'}]});assert.equal(authority.authorized,true);const quorum=evaluateQuorum({members:[{id:'a'},{id:'b'},{id:'c'}],votes:[{memberId:'a',vote:'yes'},{memberId:'b',vote:'yes'}],quorum:.5,threshold:.5});assert.equal(quorum.approved,true);const conflicts=detectConflicts({participants:[{id:'chair'}],disclosures:[],matter:{id:'m'}});const record=createDecisionRecord({id:'dec1',matter:{id:'m'},authority,evidence:['brief'],outcome:'approved',resultingMissions:['mission-1']});const ledger=appendGovernanceLedger({record});assert.equal(verifyGovernanceLedger(ledger).valid,true);assert.equal(governanceDecisionView({record,authorityResult:authority,quorumResult:quorum,conflicts}).status,'governed-ready');ledger[0].record.outcome='tampered';assert.equal(verifyGovernanceLedger(ledger).valid,false)})
