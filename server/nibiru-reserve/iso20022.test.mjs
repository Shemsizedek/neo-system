import test from'node:test';import assert from'node:assert/strict';import crypto from'node:crypto';import{validatePain001Xsd}from'./iso20022.mjs';
test('XSD gate requires pinned schema bytes and actual validator',async()=>{const schema=Buffer.from('<schema/>'),hash=crypto.createHash('sha256').update(schema).digest('hex');assert.equal((await validatePain001Xsd('<Document/>',{schemaBytes:schema,expectedSha256:'bad',validator:async()=>({valid:true})})).validationLevel,'SCHEMA_HASH_MISMATCH');assert.equal((await validatePain001Xsd('<Document/>',{schemaBytes:schema,expectedSha256:hash})).validationLevel,'VALIDATOR_UNAVAILABLE');const ok=await validatePain001Xsd('<Document/>',{schemaBytes:schema,expectedSha256:hash,validator:async()=>({valid:true,errors:[]})});assert.equal(ok.officialXsdValidated,true)});
