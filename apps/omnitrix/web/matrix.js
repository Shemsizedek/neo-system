(()=>{const c=document.createElement('canvas');c.id='matrixRain';document.body.prepend(c);const ctx=c.getContext('2d');const glyphs='01NEOOMNITRIXBTCXCPABCDEFGHIJKLMNOPQRSTUVWXYZ';let drops=[],cols=0;function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);cols=Math.ceil(innerWidth/20);drops=Array.from({length:cols},()=>Math.random()*-60)}function draw(){ctx.fillStyle='rgba(0,4,2,.08)';ctx.fillRect(0,0,innerWidth,innerHeight);ctx.fillStyle='#39ff6d';ctx.font='14px ui-monospace,SFMono-Regular,Menlo,monospace';for(let i=0;i<cols;i++){const ch=glyphs[Math.floor(Math.random()*glyphs.length)],x=i*20,y=drops[i]*20;ctx.fillText(ch,x,y);if(y>innerHeight&&Math.random()>.975)drops[i]=0;drops[i]++}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();})();
