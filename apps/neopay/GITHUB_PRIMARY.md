# NEOpay source-of-truth policy

GitHub repository `Shemsizedek/neo-system` is the primary source of truth for NEOpay development.

Base44 remains a secondary backup and prototyping environment.

Production changes should originate in GitHub branches, be reviewed through pull requests, and merge into `main` only after validation.
